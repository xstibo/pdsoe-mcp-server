# PDSOE MCP Server

An Eclipse OSGi plugin that embeds an [MCP (Model Context Protocol)](https://modelcontextprotocol.io) server inside **Progress Developer Studio for OpenEdge (PDSOE)**. When Eclipse starts, the plugin launches a local HTTP server that exposes your ABL workspace to AI assistants like Claude.

> **Note:** This plugin's code is 100% generated by [Claude Code](https://claude.ai/code). It runs and is verified live against PDSOE 12.8, but it may still contain bugs or rough edges — use it at your own discretion, and please [open an issue](../../issues) if you hit one.

## What it does

Once installed, AI assistants can interact with your PDSOE workspace directly — listing projects, reading and editing ABL source files, running builds, checking error markers, and navigating the cross-reference symbol graph — without you copy-pasting code back and forth.

## Prerequisites

- Progress Developer Studio for OpenEdge (PDSOE) — Eclipse-based IDE
- Java 17+
- [Claude Code](https://claude.ai/code) or any MCP-compatible AI client

## Installing the prebuilt plugin (quickest)

Download the latest `com.github.xstibo.pdsoe.mcp_<version>.jar` from the
[**Releases**](../../releases) page and drop it into your PDSOE installation's `dropins/` folder
(delete any older copy first), then restart PDSOE. Confirm startup via **Window → Show View →
Error Log** — you should see `PDSOE MCP Server started on http://127.0.0.1:8123/mcp (52 tools)`.

## Building from source

This is an Eclipse PDE project — there's no Maven/Gradle/CLI build; everything happens in Eclipse.

1. **Import** — clone the repo and import it into PDSOE/Eclipse (**File → Import → Existing Projects into Workspace**).
2. **Build** — **Project → Build Project** (or `Alt+B`). Compiled classes go to `bin/` (gitignored).
3. **Run it**, one of two ways:
   - **Develop / smoke-test** — **Run → Run As → Eclipse Application** launches a child IDE with the plugin active.
   - **Install into your PDSOE** — **File → Export → Plug-in Development → Deployable plug-ins and fragments**, select `com.github.xstibo.pdsoe.mcp`, and finish to produce `plugins/com.github.xstibo.pdsoe.mcp_<version>.jar`. Drop that JAR into your PDSOE installation's `dropins/` folder (delete any older copy first — the version qualifier changes each export) and restart PDSOE.

Either way, confirm startup via **Window → Show View → Error Log** — you should see `PDSOE MCP Server started on http://127.0.0.1:8123/mcp (52 tools)`.

## Connecting Claude Code

Run this once from your **ABL workspace directory** (the project root you want to work on with Claude):

```bash
claude mcp add pdsoe-mcp --transport http http://127.0.0.1:8123/mcp -s local
```

The `-s` flag controls the scope:

| Scope | Flag | Stored in | Visible |
|---|---|---|---|
| This directory only | `-s local` | `~/.claude.json` | Only when Claude Code is opened here |
| All your projects | `-s user` | `~/.claude.json` | Every Claude Code session |
| Shared with the team | `-s project` | `.mcp.json` in the repo | Anyone who opens this repo |

For most setups, `-s local` per ABL workspace is the right choice. Verify with `claude mcp list` — you should see `pdsoe-mcp: http://127.0.0.1:8123/mcp (HTTP) - ✓ Connected`.

## Security

The server binds to **`127.0.0.1` (loopback) only** with **no authentication** — it is not network-reachable, which is appropriate for a single-developer machine. Because the tools can read, write, and delete files and run builds, **do not bind it to `0.0.0.0` or otherwise expose the port without first adding authentication.**

Inputs are hardened: caller paths are constrained to the project (`..` is rejected) and all XML is parsed XXE-safe (DOCTYPE/external entities disabled).

## Settings

**Window → Preferences → PDSOE MCP Server**:

- **Enable HTTP MCP Server** — turn the server on or off (on by default).
- **Port** — the loopback port the server binds (default `8123`).
- **Server status** — shows whether the server is currently running.
- **Tools** — a checkbox tree of all tools grouped by domain. Uncheck individual tools (or a whole domain) to stop the server registering them; everything is enabled by default.

Changes take effect when you press **Apply** (or **Apply and Close**); the server restarts with the new settings, no IDE restart needed.

## Available tools

52 tools across workspace navigation, code reading, the cross-reference symbol graph, editor state, diagnostics & build, code editing, and file history.

### Workspace & file navigation

| Tool | Description |
|---|---|
| `list_projects` | Lists all projects in the workspace (open/closed) |
| `list_abl_files` | Lists all ABL source files (`.p`, `.cls`, `.i`, `.w`, `.t`) in a project |
| `list_all_files` | Lists all files in a project, optionally filtered by extension |
| `get_project_info` | Returns project natures, description, and persistent properties |
| `get_project_layout` | Returns a tree-structured view of all files and folders in a project |
| `get_propath` | Returns the configured PROPATH for a project (parses `.propath`, resolves `@{ROOT}`) |
| `get_file_info` | Returns file metadata: exists, size, last modified, line count |
| `search_files` | Finds files by name or glob pattern (e.g. `*.p`, `src/**/*.cls`) |
| `file_search_regexp` | Finds files whose names match a Java regular expression |
| `search_in_files` | Searches for text or regex across files; returns `file:line: context` (capped at 500) |
| `list_includes` | Lists the `.i` files directly included by an ABL source file |
| `find_files_including` | Lists all ABL files that include a given `.i` file (reverse of `list_includes`) |
| `delete_file` | Deletes a file (recorded in local history) |
| `move_file` | Moves or renames a file within the workspace |
| `copy_file` | Duplicates a file within the workspace |
| `create_folder` | Creates a directory (and any missing parent directories) |
| `delete_folder` | Deletes a folder and all its contents |
| `refresh_project` | Re-syncs a project with the filesystem after external changes |

### Reading code

| Tool | Description |
|---|---|
| `read_file` | Reads the full content of a workspace file (max 1 MB) |
| `read_lines` | Reads a specific line range from a file (1-based, inclusive) |
| `get_file_outline` | Lists procedures, functions, methods, and classes defined in an ABL file |
| `get_method_source` | Returns the source of a specific procedure/function/method by name |
| `get_method_signature` | Returns the signature (header + parameters, no body) of a procedure/function/method |
| `get_xref` | Parses a compiler-generated `.xref.xml` file and returns structured cross-reference data |

### Symbol graph

Backed by ABL-compiler-generated `.xref.xml` files. Run `build_symbol_index` first for the project-wide tools.

| Tool | Description |
|---|---|
| `get_callees` | External `RUN` (`.p`) and `INVOKE` (class method) calls made by a source file |
| `build_symbol_index` | Walks all `.xref.xml` under a base path and builds an in-memory cross-file index (incremental, mtime-based) |
| `get_callers` | All files that `RUN` or `INVOKE` a given procedure/`.p` or class method (requires index) |
| `find_symbol_references` | All references to a symbol across the project (requires index) |
| `get_type_hierarchy` | Parent class, implemented interfaces, and known subclasses of an ABL class (requires index) |

### Editor state

| Tool | Description |
|---|---|
| `get_open_file` | Returns the workspace path of the currently active editor file |
| `get_cursor_position` | Returns the cursor line and column in the active text editor |
| `get_selection` | Returns the selected text and its start/end line |
| `list_open_editors` | Lists all currently open editor tabs |
| `navigate_to` | Opens a file in the Eclipse editor, optionally scrolling to a specific line |
| `save_file` | Saves a specific open editor (triggers ABL compilation in PDSOE) |
| `save_all` | Saves all dirty editors |

### Diagnostics & build

| Tool | Description |
|---|---|
| `get_markers` | Returns all errors and warnings for a project |
| `get_markers_for_file` | Returns errors and warnings for a specific file |
| `build_project` | Triggers a full or incremental build; returns resulting markers |
| `build_file` | Recompiles a single file via the ABL builder; returns only that file's markers |
| `clean_project` | Cleans a project (removes build output) and triggers a full rebuild |
| `get_console_output` | Returns the content of the last active Eclipse console |

### Code editing

| Tool | Description |
|---|---|
| `write_file` | Writes content to a workspace file, creating it if it does not exist |
| `insert_at_line` | Inserts text before a specific line |
| `replace_lines` | Replaces a range of lines with new content |
| `replace_in_file` | Finds and replaces text (or regex) in a file; returns replacement count |
| `delete_lines_in_file` | Deletes a range of lines from a file |
| `apply_patch` | Applies a unified diff patch to a file |
| `undo_edit` | Restores a file to its previous state via Eclipse local history |

### File history

_(Eclipse local-history snapshots via `IFile.getHistory()`)_

| Tool | Description |
|---|---|
| `list_file_history` | Lists local-history snapshots (index + timestamp) for a file |
| `get_file_history_content` | Reads the content of a specific local-history snapshot by index |
| `diff_file` | Shows a unified diff of the current file vs. a local-history snapshot |

## How it works

Three components:

- **Jetty 12** — embedded HTTP server bound to `127.0.0.1:8123`
- **MCP Java SDK** — handles the Model Context Protocol over the `/mcp` endpoint (streamable HTTP transport)
- **Eclipse workspace APIs** — `IProject`, `IFile`, `IMarker`, `WorkspaceJob` for all ABL/workspace operations

All tool handlers are async (`McpAsyncServer` + `Mono`), so the Jetty thread is never blocked during build or file operations.

```
src/com/github/xstibo/pdsoe/mcp/
├── Activator.java          # OSGi BundleActivator — starts the plugin on IDE launch
├── McpServerManager.java   # wires Jetty + MCP SDK, aggregates every ToolProvider
└── tools/
    ├── ToolProvider.java       # interface: List<AsyncToolSpecification> tools()
    ├── ToolSupport.java        # shared helpers, schema DSL, XXE-safe parseXml, constants
    ├── WorkspaceTools.java     # file navigation, search, propath, includes, file ops (18)
    ├── ReadingTools.java       # content, line ranges, ABL outline/source/signature, xref (6)
    ├── EditorStateTools.java   # active file, cursor, selection, navigation, saving (7)
    ├── DiagnosticsTools.java   # markers, project/file build, clean, console (6)
    ├── EditingTools.java       # write, insert, replace, delete, undo, patch (7)
    ├── FileHistoryTools.java   # local-history list / read / diff (3)
    └── symbol/                 # SymbolGraphTools (5) + index data types
```

Each provider implements `ToolProvider`; `McpServerManager` flat-maps over the list, so adding a tool means editing exactly one provider.

## Roadmap

Working today: all 52 tools above, async/non-blocking handlers, ABL [proparse](https://github.com/Riverside-Software/proparse) integration, the cross-reference symbol graph, single-file builds, and JAR export/install — verified in a real PDSOE 12.8 instance.

Planned:

- [ ] Eclipse Preferences page for plugin settings:
  - [x] Enable/disable the server, with a live status line
  - [x] Configurable server port
  - [x] Tool filtering — disable individual tools or whole domains you don't need
  - [ ] File access control — restrict which projects/paths the file tools may touch
- [ ] (maybe) Automated release on tag push — needs a headless Tycho build to produce the JAR in CI
- [ ] Route routine logging to a dedicated Eclipse Console instead of the error log
- [ ] `ParseUnit` caching to skip re-parsing unchanged files
- [ ] Proparse fallback for `get_callees` (internal procedure call sites)

## Dependencies

All JARs are bundled in `lib/`. Key ones:

| JAR | Purpose |
|---|---|
| `mcp-core-1.0.0.jar` | MCP Java SDK |
| `jetty-*-12.0.14.jar` | Embedded HTTP server |
| `reactor-core-3.7.6.jar` | Async/reactive support |
| `jackson-*-2.20.jar` | JSON (Jackson 2, required by the JSON schema validator) |
| `tools-jackson-*-3.0.3.jar` | JSON (Tools Jackson 3, required by the MCP SDK) |
| `proparse-3.8.0.jar` | ABL parser (file outline / method source / signature) |

## License

MIT
